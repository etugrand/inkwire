{
  "name": "inkwire-client", "version": "0.2.0", "type": "module",
  "description": "Client SDK for publishing posts to an Inkwire receiver (idempotent, retrying, typed errors).",
  "license": "MIT",
  "repository": { "type": "git", "url": "https://github.com/etugrand/inkwire.git", "directory": "inkwire/clients/node" },
  "main": "dist/index.js", "types": "dist/index.d.ts",
  "files": ["dist/*.js", "dist/*.d.ts"],
  "scripts": { "build": "tsc", "test": "vitest run" },
  "devDependencies": { "vitest": "^2.1.0", "typescript": "^5.5.0" }
}
